// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.12.0
//   protoc               unknown
// source: littlegames/battleship/v1/battleship.proto

/* eslint-disable */

export const protobufPackage = "littlegames.battleship.v1";

export const OpCode = {
  OP_CODE_UNSPECIFIED: 0,
  /** OP_CODE_PLACE_FLEET - Client to server. */
  OP_CODE_PLACE_FLEET: 1,
  OP_CODE_FIRE: 2,
  /** OP_CODE_SNAPSHOT - Server to client. */
  OP_CODE_SNAPSHOT: 3,
  OP_CODE_REFUSED: 4,
} as const;

export type OpCode = typeof OpCode[keyof typeof OpCode];

export namespace OpCode {
  export type OP_CODE_UNSPECIFIED = typeof OpCode.OP_CODE_UNSPECIFIED;
  export type OP_CODE_PLACE_FLEET = typeof OpCode.OP_CODE_PLACE_FLEET;
  export type OP_CODE_FIRE = typeof OpCode.OP_CODE_FIRE;
  export type OP_CODE_SNAPSHOT = typeof OpCode.OP_CODE_SNAPSHOT;
  export type OP_CODE_REFUSED = typeof OpCode.OP_CODE_REFUSED;
}

export const Phase = {
  PHASE_UNSPECIFIED: 0,
  PHASE_WAITING: 1,
  PHASE_PLACEMENT: 2,
  PHASE_PLAYING: 3,
  PHASE_FINISHED: 4,
} as const;

export type Phase = typeof Phase[keyof typeof Phase];

export namespace Phase {
  export type PHASE_UNSPECIFIED = typeof Phase.PHASE_UNSPECIFIED;
  export type PHASE_WAITING = typeof Phase.PHASE_WAITING;
  export type PHASE_PLACEMENT = typeof Phase.PHASE_PLACEMENT;
  export type PHASE_PLAYING = typeof Phase.PHASE_PLAYING;
  export type PHASE_FINISHED = typeof Phase.PHASE_FINISHED;
}

export const Orientation = { ORIENTATION_UNSPECIFIED: 0, ORIENTATION_HORIZONTAL: 1, ORIENTATION_VERTICAL: 2 } as const;

export type Orientation = typeof Orientation[keyof typeof Orientation];

export namespace Orientation {
  export type ORIENTATION_UNSPECIFIED = typeof Orientation.ORIENTATION_UNSPECIFIED;
  export type ORIENTATION_HORIZONTAL = typeof Orientation.ORIENTATION_HORIZONTAL;
  export type ORIENTATION_VERTICAL = typeof Orientation.ORIENTATION_VERTICAL;
}

export const ShotResult = {
  SHOT_RESULT_UNSPECIFIED: 0,
  SHOT_RESULT_MISS: 1,
  SHOT_RESULT_HIT: 2,
  SHOT_RESULT_SUNK: 3,
} as const;

export type ShotResult = typeof ShotResult[keyof typeof ShotResult];

export namespace ShotResult {
  export type SHOT_RESULT_UNSPECIFIED = typeof ShotResult.SHOT_RESULT_UNSPECIFIED;
  export type SHOT_RESULT_MISS = typeof ShotResult.SHOT_RESULT_MISS;
  export type SHOT_RESULT_HIT = typeof ShotResult.SHOT_RESULT_HIT;
  export type SHOT_RESULT_SUNK = typeof ShotResult.SHOT_RESULT_SUNK;
}

/** Where one ship sits. Its length comes from its position in the fleet. */
export interface Placement {
  row: number;
  column: number;
  orientation: Orientation;
}

/** A cell someone fired at, and what it found. */
export interface Shot {
  row: number;
  column: number;
  result: ShotResult;
}

/**
 * Client to server: the whole fleet at once.
 *
 * All five at once rather than one ship at a time, because whether an
 * arrangement is legal is a property of the arrangement.
 */
export interface PlaceFleet {
  ships: Placement[];
}

/** Client to server: fire at a cell. */
export interface Fire {
  row: number;
  column: number;
}

/** Server to client: why something was refused, in words meant to be shown. */
export interface Refused {
  reason: string;
}

/** Server to client, built for one recipient. */
export interface Snapshot {
  phase: Phase;
  /** True when it is the recipient's turn to fire. */
  yourTurn: boolean;
  /** The recipient's own fleet, in full, and every shot taken at it. */
  yourFleet: Placement[];
  incoming: Shot[];
  youAreReady: boolean;
  /** Only the cells the recipient has fired at. Never the opponent's ships. */
  outgoing: Shot[];
  opponentReady: boolean;
  opponentPresent: boolean;
  /** How many of each side's ships are on the bottom. */
  yourShipsSunk: number;
  opponentShipsSunk: number;
  /** Set once the game is over. */
  finished: boolean;
  youWon: boolean;
}
