// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.12.0
//   protoc               unknown
// source: littlegames/arena/v1/arena.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "littlegames.arena.v1";

export const OpCode = {
  OP_CODE_UNSPECIFIED: 0,
  /** OP_CODE_PLAYER_INPUT - Client to server. */
  OP_CODE_PLAYER_INPUT: 1,
  /** OP_CODE_SNAPSHOT - Server to client. */
  OP_CODE_SNAPSHOT: 2,
} as const;

export type OpCode = typeof OpCode[keyof typeof OpCode];

export namespace OpCode {
  export type OP_CODE_UNSPECIFIED = typeof OpCode.OP_CODE_UNSPECIFIED;
  export type OP_CODE_PLAYER_INPUT = typeof OpCode.OP_CODE_PLAYER_INPUT;
  export type OP_CODE_SNAPSHOT = typeof OpCode.OP_CODE_SNAPSHOT;
}

export function opCodeFromJSON(object: any): OpCode {
  switch (object) {
    case 0:
    case "OP_CODE_UNSPECIFIED":
      return OpCode.OP_CODE_UNSPECIFIED;
    case 1:
    case "OP_CODE_PLAYER_INPUT":
      return OpCode.OP_CODE_PLAYER_INPUT;
    case 2:
    case "OP_CODE_SNAPSHOT":
      return OpCode.OP_CODE_SNAPSHOT;
    default:
      throw new globalThis.Error("Unrecognized enum value " + object + " for enum OpCode");
  }
}

export function opCodeToJSON(object: OpCode): string {
  switch (object) {
    case OpCode.OP_CODE_UNSPECIFIED:
      return "OP_CODE_UNSPECIFIED";
    case OpCode.OP_CODE_PLAYER_INPUT:
      return "OP_CODE_PLAYER_INPUT";
    case OpCode.OP_CODE_SNAPSHOT:
      return "OP_CODE_SNAPSHOT";
    default:
      throw new globalThis.Error("Unrecognized enum value " + object + " for enum OpCode");
  }
}

export const Phase = {
  PHASE_UNSPECIFIED: 0,
  PHASE_WAITING: 1,
  PHASE_COUNTDOWN: 2,
  PHASE_PLAYING: 3,
  PHASE_FINISHED: 4,
} as const;

export type Phase = typeof Phase[keyof typeof Phase];

export namespace Phase {
  export type PHASE_UNSPECIFIED = typeof Phase.PHASE_UNSPECIFIED;
  export type PHASE_WAITING = typeof Phase.PHASE_WAITING;
  export type PHASE_COUNTDOWN = typeof Phase.PHASE_COUNTDOWN;
  export type PHASE_PLAYING = typeof Phase.PHASE_PLAYING;
  export type PHASE_FINISHED = typeof Phase.PHASE_FINISHED;
}

export function phaseFromJSON(object: any): Phase {
  switch (object) {
    case 0:
    case "PHASE_UNSPECIFIED":
      return Phase.PHASE_UNSPECIFIED;
    case 1:
    case "PHASE_WAITING":
      return Phase.PHASE_WAITING;
    case 2:
    case "PHASE_COUNTDOWN":
      return Phase.PHASE_COUNTDOWN;
    case 3:
    case "PHASE_PLAYING":
      return Phase.PHASE_PLAYING;
    case 4:
    case "PHASE_FINISHED":
      return Phase.PHASE_FINISHED;
    default:
      throw new globalThis.Error("Unrecognized enum value " + object + " for enum Phase");
  }
}

export function phaseToJSON(object: Phase): string {
  switch (object) {
    case Phase.PHASE_UNSPECIFIED:
      return "PHASE_UNSPECIFIED";
    case Phase.PHASE_WAITING:
      return "PHASE_WAITING";
    case Phase.PHASE_COUNTDOWN:
      return "PHASE_COUNTDOWN";
    case Phase.PHASE_PLAYING:
      return "PHASE_PLAYING";
    case Phase.PHASE_FINISHED:
      return "PHASE_FINISHED";
    default:
      throw new globalThis.Error("Unrecognized enum value " + object + " for enum Phase");
  }
}

/** Which half of the arena a player holds. */
export const Seat = { SEAT_UNSPECIFIED: 0, SEAT_NORTH: 1, SEAT_SOUTH: 2 } as const;

export type Seat = typeof Seat[keyof typeof Seat];

export namespace Seat {
  export type SEAT_UNSPECIFIED = typeof Seat.SEAT_UNSPECIFIED;
  export type SEAT_NORTH = typeof Seat.SEAT_NORTH;
  export type SEAT_SOUTH = typeof Seat.SEAT_SOUTH;
}

export function seatFromJSON(object: any): Seat {
  switch (object) {
    case 0:
    case "SEAT_UNSPECIFIED":
      return Seat.SEAT_UNSPECIFIED;
    case 1:
    case "SEAT_NORTH":
      return Seat.SEAT_NORTH;
    case 2:
    case "SEAT_SOUTH":
      return Seat.SEAT_SOUTH;
    default:
      throw new globalThis.Error("Unrecognized enum value " + object + " for enum Seat");
  }
}

export function seatToJSON(object: Seat): string {
  switch (object) {
    case Seat.SEAT_UNSPECIFIED:
      return "SEAT_UNSPECIFIED";
    case Seat.SEAT_NORTH:
      return "SEAT_NORTH";
    case Seat.SEAT_SOUTH:
      return "SEAT_SOUTH";
    default:
      throw new globalThis.Error("Unrecognized enum value " + object + " for enum Seat");
  }
}

/**
 * One tick of intent, sent from client to server.
 *
 * No angle appears anywhere: the client resolves its own yaw and pitch into
 * world-space directions and sends those, which is what keeps sine and cosine
 * out of a simulation that has to agree with a second implementation to the
 * last bit. It gives nothing away — a client that can look anywhere could
 * already move in any direction, and the only thing direction bits ever bought
 * was a speed cap, which the server applies to this vector directly.
 */
export interface PlayerInput {
  /**
   * Monotonically increasing, assigned by the client.
   *
   * Echoed back in the snapshot, which is what lets the client drop
   * acknowledged commands and replay the rest on top of authoritative state.
   */
  seq: number;
  /**
   * Movement intent across and along the arena, on a scale of 1024. At most
   * unit length once dequantised; anything longer is shortened by the server.
   */
  moveX: number;
  moveZ: number;
  /**
   * Where the player is looking, as a world-space vector on a scale of 8192.
   * Normalised by the server, so its length carries no meaning.
   */
  aimX: number;
  aimY: number;
  aimZ: number;
  jump: boolean;
  crouch: boolean;
  /**
   * Aiming down the sights. Nothing in the rules reads it; it travels so the
   * opponent can be drawn doing it.
   */
  zoomed: boolean;
  /**
   * Newest server tick this client had drawn when it sampled this command.
   *
   * How far the server rewinds the target to judge a shot. Clamped against the
   * ticks the server knows it broadcast, so it cannot buy time by claiming to
   * have seen the distant past; claiming to have seen less than it did is
   * allowed and only makes the shot resolve against fresher positions.
   */
  seenTick: number;
  /**
   * How many shots this client has fired in total, ever.
   *
   * A counter rather than a flag, for the same reason `seq` is one: a duplicate
   * delivery re-sends a number the server has already reached and fires
   * nothing, and a lost command is made good by the next one that arrives
   * rather than costing a shot. A flag would do neither.
   */
  shotsFired: number;
}

/**
 * A body, as the server holds it. Exact doubles, in metres.
 *
 * Every field the simulation needs to advance from here is present, because
 * this is where a client's prediction restarts: replaying unacknowledged
 * commands from a body missing its vertical speed would put the player back on
 * the ground mid-jump.
 */
export interface Body {
  /** Feet, not eyes. */
  x: number;
  y: number;
  z: number;
  /** Vertical speed. There is no horizontal inertia to carry. */
  vy: number;
  grounded: boolean;
  crouching: boolean;
}

/** A unit vector. */
export interface Vector3 {
  x: number;
  y: number;
  z: number;
}

/** One player, as the server sees them. */
export interface PlayerState {
  userId: string;
  username: string;
  seat: Seat;
  /** Highest input sequence number the server has applied for this player. */
  lastProcessedSeq: number;
  body: Body | undefined;
  aim: Vector3 | undefined;
  alive: boolean;
  score: number;
  /** Ticks left before this player is put back at their spawn. */
  respawnTicks: number;
  /** Ticks left before they may fire again. */
  cooldownTicks: number;
  /**
   * Bumped every time this player respawns.
   *
   * The server refuses to rewind a target across one, so nobody is killed a
   * beat after reappearing. The client throws away its unacknowledged commands
   * when its own changes, so it does not replay a dead player's movement onto a
   * fresh spawn and skate out of it.
   */
  spawnEpoch: number;
  zoomed: boolean;
}

/** A shot the server resolved, for the client to draw. */
export interface ShotEvent {
  /**
   * Unique and increasing within a match, so a client can tell a shot it has
   * already drawn from one it has not.
   */
  id: number;
  shooter: Seat;
  origin:
    | Vector3
    | undefined;
  /** Where it stopped: a body, a wall, or the end of its range. */
  endpoint: Vector3 | undefined;
  hitPlayer: boolean;
}

/** The authoritative state, broadcast once per tick. */
export interface Snapshot {
  /** Server tick this state was produced on. */
  tick: number;
  phase: Phase;
  /** Ticks left in the countdown, zero outside it. */
  phaseTicks: number;
  players: PlayerState[];
  /** Set once the match is over. */
  winner: Seat;
  /**
   * The last few shots fired, not only this tick's.
   *
   * Snapshots travel unreliably, and a tracer that appeared in one frame alone
   * would sometimes never be seen at all. A window of them means a client that
   * dropped a snapshot still draws the shot, and the ids tell it which ones it
   * has already drawn.
   */
  shots: ShotEvent[];
}

function createBasePlayerInput(): PlayerInput {
  return {
    seq: 0,
    moveX: 0,
    moveZ: 0,
    aimX: 0,
    aimY: 0,
    aimZ: 0,
    jump: false,
    crouch: false,
    zoomed: false,
    seenTick: 0,
    shotsFired: 0,
  };
}

export const PlayerInput: MessageFns<PlayerInput> = {
  encode(message: PlayerInput, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.seq !== 0) {
      writer.uint32(8).uint32(message.seq);
    }
    if (message.moveX !== 0) {
      writer.uint32(16).sint32(message.moveX);
    }
    if (message.moveZ !== 0) {
      writer.uint32(24).sint32(message.moveZ);
    }
    if (message.aimX !== 0) {
      writer.uint32(32).sint32(message.aimX);
    }
    if (message.aimY !== 0) {
      writer.uint32(40).sint32(message.aimY);
    }
    if (message.aimZ !== 0) {
      writer.uint32(48).sint32(message.aimZ);
    }
    if (message.jump !== false) {
      writer.uint32(56).bool(message.jump);
    }
    if (message.crouch !== false) {
      writer.uint32(64).bool(message.crouch);
    }
    if (message.zoomed !== false) {
      writer.uint32(72).bool(message.zoomed);
    }
    if (message.seenTick !== 0) {
      writer.uint32(80).uint32(message.seenTick);
    }
    if (message.shotsFired !== 0) {
      writer.uint32(88).uint32(message.shotsFired);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PlayerInput {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePlayerInput();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.seq = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.moveX = reader.sint32();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.moveZ = reader.sint32();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.aimX = reader.sint32();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.aimY = reader.sint32();
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.aimZ = reader.sint32();
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.jump = reader.bool();
          continue;
        }
        case 8: {
          if (tag !== 64) {
            break;
          }

          message.crouch = reader.bool();
          continue;
        }
        case 9: {
          if (tag !== 72) {
            break;
          }

          message.zoomed = reader.bool();
          continue;
        }
        case 10: {
          if (tag !== 80) {
            break;
          }

          message.seenTick = reader.uint32();
          continue;
        }
        case 11: {
          if (tag !== 88) {
            break;
          }

          message.shotsFired = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PlayerInput {
    return {
      seq: isSet(object.seq) ? globalThis.Number(object.seq) : 0,
      moveX: isSet(object.moveX)
        ? globalThis.Number(object.moveX)
        : isSet(object.move_x)
        ? globalThis.Number(object.move_x)
        : 0,
      moveZ: isSet(object.moveZ)
        ? globalThis.Number(object.moveZ)
        : isSet(object.move_z)
        ? globalThis.Number(object.move_z)
        : 0,
      aimX: isSet(object.aimX)
        ? globalThis.Number(object.aimX)
        : isSet(object.aim_x)
        ? globalThis.Number(object.aim_x)
        : 0,
      aimY: isSet(object.aimY)
        ? globalThis.Number(object.aimY)
        : isSet(object.aim_y)
        ? globalThis.Number(object.aim_y)
        : 0,
      aimZ: isSet(object.aimZ)
        ? globalThis.Number(object.aimZ)
        : isSet(object.aim_z)
        ? globalThis.Number(object.aim_z)
        : 0,
      jump: isSet(object.jump) ? globalThis.Boolean(object.jump) : false,
      crouch: isSet(object.crouch) ? globalThis.Boolean(object.crouch) : false,
      zoomed: isSet(object.zoomed) ? globalThis.Boolean(object.zoomed) : false,
      seenTick: isSet(object.seenTick)
        ? globalThis.Number(object.seenTick)
        : isSet(object.seen_tick)
        ? globalThis.Number(object.seen_tick)
        : 0,
      shotsFired: isSet(object.shotsFired)
        ? globalThis.Number(object.shotsFired)
        : isSet(object.shots_fired)
        ? globalThis.Number(object.shots_fired)
        : 0,
    };
  },

  toJSON(message: PlayerInput): unknown {
    const obj: any = {};
    if (message.seq !== 0) {
      obj.seq = Math.round(message.seq);
    }
    if (message.moveX !== 0) {
      obj.moveX = Math.round(message.moveX);
    }
    if (message.moveZ !== 0) {
      obj.moveZ = Math.round(message.moveZ);
    }
    if (message.aimX !== 0) {
      obj.aimX = Math.round(message.aimX);
    }
    if (message.aimY !== 0) {
      obj.aimY = Math.round(message.aimY);
    }
    if (message.aimZ !== 0) {
      obj.aimZ = Math.round(message.aimZ);
    }
    if (message.jump !== false) {
      obj.jump = message.jump;
    }
    if (message.crouch !== false) {
      obj.crouch = message.crouch;
    }
    if (message.zoomed !== false) {
      obj.zoomed = message.zoomed;
    }
    if (message.seenTick !== 0) {
      obj.seenTick = Math.round(message.seenTick);
    }
    if (message.shotsFired !== 0) {
      obj.shotsFired = Math.round(message.shotsFired);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<PlayerInput>, I>>(base?: I): PlayerInput {
    return PlayerInput.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PlayerInput>, I>>(object: I): PlayerInput {
    const message = createBasePlayerInput();
    message.seq = object.seq ?? 0;
    message.moveX = object.moveX ?? 0;
    message.moveZ = object.moveZ ?? 0;
    message.aimX = object.aimX ?? 0;
    message.aimY = object.aimY ?? 0;
    message.aimZ = object.aimZ ?? 0;
    message.jump = object.jump ?? false;
    message.crouch = object.crouch ?? false;
    message.zoomed = object.zoomed ?? false;
    message.seenTick = object.seenTick ?? 0;
    message.shotsFired = object.shotsFired ?? 0;
    return message;
  },
};

function createBaseBody(): Body {
  return { x: 0, y: 0, z: 0, vy: 0, grounded: false, crouching: false };
}

export const Body: MessageFns<Body> = {
  encode(message: Body, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.x !== 0) {
      writer.uint32(9).double(message.x);
    }
    if (message.y !== 0) {
      writer.uint32(17).double(message.y);
    }
    if (message.z !== 0) {
      writer.uint32(25).double(message.z);
    }
    if (message.vy !== 0) {
      writer.uint32(33).double(message.vy);
    }
    if (message.grounded !== false) {
      writer.uint32(40).bool(message.grounded);
    }
    if (message.crouching !== false) {
      writer.uint32(48).bool(message.crouching);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Body {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseBody();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 9) {
            break;
          }

          message.x = reader.double();
          continue;
        }
        case 2: {
          if (tag !== 17) {
            break;
          }

          message.y = reader.double();
          continue;
        }
        case 3: {
          if (tag !== 25) {
            break;
          }

          message.z = reader.double();
          continue;
        }
        case 4: {
          if (tag !== 33) {
            break;
          }

          message.vy = reader.double();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.grounded = reader.bool();
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.crouching = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Body {
    return {
      x: isSet(object.x) ? globalThis.Number(object.x) : 0,
      y: isSet(object.y) ? globalThis.Number(object.y) : 0,
      z: isSet(object.z) ? globalThis.Number(object.z) : 0,
      vy: isSet(object.vy) ? globalThis.Number(object.vy) : 0,
      grounded: isSet(object.grounded) ? globalThis.Boolean(object.grounded) : false,
      crouching: isSet(object.crouching) ? globalThis.Boolean(object.crouching) : false,
    };
  },

  toJSON(message: Body): unknown {
    const obj: any = {};
    if (message.x !== 0) {
      obj.x = message.x;
    }
    if (message.y !== 0) {
      obj.y = message.y;
    }
    if (message.z !== 0) {
      obj.z = message.z;
    }
    if (message.vy !== 0) {
      obj.vy = message.vy;
    }
    if (message.grounded !== false) {
      obj.grounded = message.grounded;
    }
    if (message.crouching !== false) {
      obj.crouching = message.crouching;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Body>, I>>(base?: I): Body {
    return Body.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Body>, I>>(object: I): Body {
    const message = createBaseBody();
    message.x = object.x ?? 0;
    message.y = object.y ?? 0;
    message.z = object.z ?? 0;
    message.vy = object.vy ?? 0;
    message.grounded = object.grounded ?? false;
    message.crouching = object.crouching ?? false;
    return message;
  },
};

function createBaseVector3(): Vector3 {
  return { x: 0, y: 0, z: 0 };
}

export const Vector3: MessageFns<Vector3> = {
  encode(message: Vector3, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.x !== 0) {
      writer.uint32(9).double(message.x);
    }
    if (message.y !== 0) {
      writer.uint32(17).double(message.y);
    }
    if (message.z !== 0) {
      writer.uint32(25).double(message.z);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Vector3 {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseVector3();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 9) {
            break;
          }

          message.x = reader.double();
          continue;
        }
        case 2: {
          if (tag !== 17) {
            break;
          }

          message.y = reader.double();
          continue;
        }
        case 3: {
          if (tag !== 25) {
            break;
          }

          message.z = reader.double();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Vector3 {
    return {
      x: isSet(object.x) ? globalThis.Number(object.x) : 0,
      y: isSet(object.y) ? globalThis.Number(object.y) : 0,
      z: isSet(object.z) ? globalThis.Number(object.z) : 0,
    };
  },

  toJSON(message: Vector3): unknown {
    const obj: any = {};
    if (message.x !== 0) {
      obj.x = message.x;
    }
    if (message.y !== 0) {
      obj.y = message.y;
    }
    if (message.z !== 0) {
      obj.z = message.z;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Vector3>, I>>(base?: I): Vector3 {
    return Vector3.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Vector3>, I>>(object: I): Vector3 {
    const message = createBaseVector3();
    message.x = object.x ?? 0;
    message.y = object.y ?? 0;
    message.z = object.z ?? 0;
    return message;
  },
};

function createBasePlayerState(): PlayerState {
  return {
    userId: "",
    username: "",
    seat: 0,
    lastProcessedSeq: 0,
    body: undefined,
    aim: undefined,
    alive: false,
    score: 0,
    respawnTicks: 0,
    cooldownTicks: 0,
    spawnEpoch: 0,
    zoomed: false,
  };
}

export const PlayerState: MessageFns<PlayerState> = {
  encode(message: PlayerState, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.userId !== "") {
      writer.uint32(10).string(message.userId);
    }
    if (message.username !== "") {
      writer.uint32(18).string(message.username);
    }
    if (message.seat !== 0) {
      writer.uint32(24).int32(message.seat);
    }
    if (message.lastProcessedSeq !== 0) {
      writer.uint32(32).uint32(message.lastProcessedSeq);
    }
    if (message.body !== undefined) {
      Body.encode(message.body, writer.uint32(42).fork()).join();
    }
    if (message.aim !== undefined) {
      Vector3.encode(message.aim, writer.uint32(50).fork()).join();
    }
    if (message.alive !== false) {
      writer.uint32(56).bool(message.alive);
    }
    if (message.score !== 0) {
      writer.uint32(64).uint32(message.score);
    }
    if (message.respawnTicks !== 0) {
      writer.uint32(72).uint32(message.respawnTicks);
    }
    if (message.cooldownTicks !== 0) {
      writer.uint32(80).uint32(message.cooldownTicks);
    }
    if (message.spawnEpoch !== 0) {
      writer.uint32(88).uint32(message.spawnEpoch);
    }
    if (message.zoomed !== false) {
      writer.uint32(96).bool(message.zoomed);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PlayerState {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePlayerState();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.userId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.username = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.seat = reader.int32() as any;
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.lastProcessedSeq = reader.uint32();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.body = Body.decode(reader, reader.uint32());
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.aim = Vector3.decode(reader, reader.uint32());
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.alive = reader.bool();
          continue;
        }
        case 8: {
          if (tag !== 64) {
            break;
          }

          message.score = reader.uint32();
          continue;
        }
        case 9: {
          if (tag !== 72) {
            break;
          }

          message.respawnTicks = reader.uint32();
          continue;
        }
        case 10: {
          if (tag !== 80) {
            break;
          }

          message.cooldownTicks = reader.uint32();
          continue;
        }
        case 11: {
          if (tag !== 88) {
            break;
          }

          message.spawnEpoch = reader.uint32();
          continue;
        }
        case 12: {
          if (tag !== 96) {
            break;
          }

          message.zoomed = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PlayerState {
    return {
      userId: isSet(object.userId)
        ? globalThis.String(object.userId)
        : isSet(object.user_id)
        ? globalThis.String(object.user_id)
        : "",
      username: isSet(object.username) ? globalThis.String(object.username) : "",
      seat: isSet(object.seat) ? seatFromJSON(object.seat) : 0,
      lastProcessedSeq: isSet(object.lastProcessedSeq)
        ? globalThis.Number(object.lastProcessedSeq)
        : isSet(object.last_processed_seq)
        ? globalThis.Number(object.last_processed_seq)
        : 0,
      body: isSet(object.body) ? Body.fromJSON(object.body) : undefined,
      aim: isSet(object.aim) ? Vector3.fromJSON(object.aim) : undefined,
      alive: isSet(object.alive) ? globalThis.Boolean(object.alive) : false,
      score: isSet(object.score) ? globalThis.Number(object.score) : 0,
      respawnTicks: isSet(object.respawnTicks)
        ? globalThis.Number(object.respawnTicks)
        : isSet(object.respawn_ticks)
        ? globalThis.Number(object.respawn_ticks)
        : 0,
      cooldownTicks: isSet(object.cooldownTicks)
        ? globalThis.Number(object.cooldownTicks)
        : isSet(object.cooldown_ticks)
        ? globalThis.Number(object.cooldown_ticks)
        : 0,
      spawnEpoch: isSet(object.spawnEpoch)
        ? globalThis.Number(object.spawnEpoch)
        : isSet(object.spawn_epoch)
        ? globalThis.Number(object.spawn_epoch)
        : 0,
      zoomed: isSet(object.zoomed) ? globalThis.Boolean(object.zoomed) : false,
    };
  },

  toJSON(message: PlayerState): unknown {
    const obj: any = {};
    if (message.userId !== "") {
      obj.userId = message.userId;
    }
    if (message.username !== "") {
      obj.username = message.username;
    }
    if (message.seat !== 0) {
      obj.seat = seatToJSON(message.seat);
    }
    if (message.lastProcessedSeq !== 0) {
      obj.lastProcessedSeq = Math.round(message.lastProcessedSeq);
    }
    if (message.body !== undefined) {
      obj.body = Body.toJSON(message.body);
    }
    if (message.aim !== undefined) {
      obj.aim = Vector3.toJSON(message.aim);
    }
    if (message.alive !== false) {
      obj.alive = message.alive;
    }
    if (message.score !== 0) {
      obj.score = Math.round(message.score);
    }
    if (message.respawnTicks !== 0) {
      obj.respawnTicks = Math.round(message.respawnTicks);
    }
    if (message.cooldownTicks !== 0) {
      obj.cooldownTicks = Math.round(message.cooldownTicks);
    }
    if (message.spawnEpoch !== 0) {
      obj.spawnEpoch = Math.round(message.spawnEpoch);
    }
    if (message.zoomed !== false) {
      obj.zoomed = message.zoomed;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<PlayerState>, I>>(base?: I): PlayerState {
    return PlayerState.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PlayerState>, I>>(object: I): PlayerState {
    const message = createBasePlayerState();
    message.userId = object.userId ?? "";
    message.username = object.username ?? "";
    message.seat = object.seat ?? 0;
    message.lastProcessedSeq = object.lastProcessedSeq ?? 0;
    message.body = (object.body !== undefined && object.body !== null) ? Body.fromPartial(object.body) : undefined;
    message.aim = (object.aim !== undefined && object.aim !== null) ? Vector3.fromPartial(object.aim) : undefined;
    message.alive = object.alive ?? false;
    message.score = object.score ?? 0;
    message.respawnTicks = object.respawnTicks ?? 0;
    message.cooldownTicks = object.cooldownTicks ?? 0;
    message.spawnEpoch = object.spawnEpoch ?? 0;
    message.zoomed = object.zoomed ?? false;
    return message;
  },
};

function createBaseShotEvent(): ShotEvent {
  return { id: 0, shooter: 0, origin: undefined, endpoint: undefined, hitPlayer: false };
}

export const ShotEvent: MessageFns<ShotEvent> = {
  encode(message: ShotEvent, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== 0) {
      writer.uint32(8).uint32(message.id);
    }
    if (message.shooter !== 0) {
      writer.uint32(16).int32(message.shooter);
    }
    if (message.origin !== undefined) {
      Vector3.encode(message.origin, writer.uint32(26).fork()).join();
    }
    if (message.endpoint !== undefined) {
      Vector3.encode(message.endpoint, writer.uint32(34).fork()).join();
    }
    if (message.hitPlayer !== false) {
      writer.uint32(40).bool(message.hitPlayer);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ShotEvent {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseShotEvent();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.id = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.shooter = reader.int32() as any;
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.origin = Vector3.decode(reader, reader.uint32());
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.endpoint = Vector3.decode(reader, reader.uint32());
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.hitPlayer = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ShotEvent {
    return {
      id: isSet(object.id) ? globalThis.Number(object.id) : 0,
      shooter: isSet(object.shooter) ? seatFromJSON(object.shooter) : 0,
      origin: isSet(object.origin) ? Vector3.fromJSON(object.origin) : undefined,
      endpoint: isSet(object.endpoint) ? Vector3.fromJSON(object.endpoint) : undefined,
      hitPlayer: isSet(object.hitPlayer)
        ? globalThis.Boolean(object.hitPlayer)
        : isSet(object.hit_player)
        ? globalThis.Boolean(object.hit_player)
        : false,
    };
  },

  toJSON(message: ShotEvent): unknown {
    const obj: any = {};
    if (message.id !== 0) {
      obj.id = Math.round(message.id);
    }
    if (message.shooter !== 0) {
      obj.shooter = seatToJSON(message.shooter);
    }
    if (message.origin !== undefined) {
      obj.origin = Vector3.toJSON(message.origin);
    }
    if (message.endpoint !== undefined) {
      obj.endpoint = Vector3.toJSON(message.endpoint);
    }
    if (message.hitPlayer !== false) {
      obj.hitPlayer = message.hitPlayer;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ShotEvent>, I>>(base?: I): ShotEvent {
    return ShotEvent.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ShotEvent>, I>>(object: I): ShotEvent {
    const message = createBaseShotEvent();
    message.id = object.id ?? 0;
    message.shooter = object.shooter ?? 0;
    message.origin = (object.origin !== undefined && object.origin !== null)
      ? Vector3.fromPartial(object.origin)
      : undefined;
    message.endpoint = (object.endpoint !== undefined && object.endpoint !== null)
      ? Vector3.fromPartial(object.endpoint)
      : undefined;
    message.hitPlayer = object.hitPlayer ?? false;
    return message;
  },
};

function createBaseSnapshot(): Snapshot {
  return { tick: 0, phase: 0, phaseTicks: 0, players: [], winner: 0, shots: [] };
}

export const Snapshot: MessageFns<Snapshot> = {
  encode(message: Snapshot, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.tick !== 0) {
      writer.uint32(8).uint32(message.tick);
    }
    if (message.phase !== 0) {
      writer.uint32(16).int32(message.phase);
    }
    if (message.phaseTicks !== 0) {
      writer.uint32(24).uint32(message.phaseTicks);
    }
    for (const v of message.players) {
      PlayerState.encode(v!, writer.uint32(34).fork()).join();
    }
    if (message.winner !== 0) {
      writer.uint32(40).int32(message.winner);
    }
    for (const v of message.shots) {
      ShotEvent.encode(v!, writer.uint32(50).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Snapshot {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSnapshot();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.tick = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.phase = reader.int32() as any;
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.phaseTicks = reader.uint32();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.players.push(PlayerState.decode(reader, reader.uint32()));
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.winner = reader.int32() as any;
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.shots.push(ShotEvent.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Snapshot {
    return {
      tick: isSet(object.tick) ? globalThis.Number(object.tick) : 0,
      phase: isSet(object.phase) ? phaseFromJSON(object.phase) : 0,
      phaseTicks: isSet(object.phaseTicks)
        ? globalThis.Number(object.phaseTicks)
        : isSet(object.phase_ticks)
        ? globalThis.Number(object.phase_ticks)
        : 0,
      players: globalThis.Array.isArray(object?.players) ? object.players.map((e: any) => PlayerState.fromJSON(e)) : [],
      winner: isSet(object.winner) ? seatFromJSON(object.winner) : 0,
      shots: globalThis.Array.isArray(object?.shots) ? object.shots.map((e: any) => ShotEvent.fromJSON(e)) : [],
    };
  },

  toJSON(message: Snapshot): unknown {
    const obj: any = {};
    if (message.tick !== 0) {
      obj.tick = Math.round(message.tick);
    }
    if (message.phase !== 0) {
      obj.phase = phaseToJSON(message.phase);
    }
    if (message.phaseTicks !== 0) {
      obj.phaseTicks = Math.round(message.phaseTicks);
    }
    if (message.players?.length) {
      obj.players = message.players.map((e) => PlayerState.toJSON(e));
    }
    if (message.winner !== 0) {
      obj.winner = seatToJSON(message.winner);
    }
    if (message.shots?.length) {
      obj.shots = message.shots.map((e) => ShotEvent.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Snapshot>, I>>(base?: I): Snapshot {
    return Snapshot.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Snapshot>, I>>(object: I): Snapshot {
    const message = createBaseSnapshot();
    message.tick = object.tick ?? 0;
    message.phase = object.phase ?? 0;
    message.phaseTicks = object.phaseTicks ?? 0;
    message.players = object.players?.map((e) => PlayerState.fromPartial(e)) || [];
    message.winner = object.winner ?? 0;
    message.shots = object.shots?.map((e) => ShotEvent.fromPartial(e)) || [];
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
